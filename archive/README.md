# Historical checkpoints

`pcb/` contains earlier routing candidates, backups and old reports/data.
`scripts/` preserves one-off development tools unchanged; their old filenames,
absolute paths and working-directory assumptions are historical, not supported.
`enclosure/` preserves the superseded enclosure source and exports.

Use `../hardware/rev-f/` and `../scripts/` for current work.
